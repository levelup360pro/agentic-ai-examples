# Local .terraform directories (provider cache)
.terraform/

# .tfstate files (Terraform state - sensitive, use remote backend)
*.tfstate
*.tfstate.*
tfstate/

# Crash log files
crash.log
crash.*.log

# Ignore override files (local customizations)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include .tfvars files by default, but ignore if they contain secrets
# (Uncomment if you have sensitive .tfvars; commit non-sensitive ones)
# *.tfvars
# *.tfvars.json

# Ignore CLI configuration and data files
.terraformrc
terraform.rc

# Dependency lock file (commit if pinning versions, ignore if not)
# Uncomment to ignore:
.terraform.lock.hcl

# Backup files
*.backup
*.bak

# Editor/IDE files (e.g., VS Code, IntelliJ)
.vscode/
.idea/
*.swp
*.swo
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
.tox/
*.cover
*.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.coveragerc

# Jupyter Notebook checkpoints and output
.ipynb_checkpoints
*.nbconvert.ipynb

# PyCharm
.idea/

# VS Code
.vscode/

# Editor backups
*~
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*
*.env

# Project-specific data (persistent vector DB)
# Keep the raw data in repo if you want, but ignore the persistent DB and heavy binaries
data/chroma_db/

# Cache directories created by tools or tests
.cache/

# Logs
logs/
*.log

# OS-generated files
.DS_Store
Thumbs.db

# Misc local files
*.log
*.tfplan
*.tfout
*.docx

Ignore local secrets or temp files
# (Customize as needed)
secrets/
*.key
*.pem
*.crt
.env
.venv
local.tfvars
creds.md

# Environment-specific files
.venv/
runs/
.ipynb_checkpoints/
